version: "3.9"

services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./videos:/app/videos
      - ./videos.json:/app/videos.json
      - ./links.json:/app/links.json
    restart: unless-stopped
